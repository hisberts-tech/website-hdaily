# Frontend environment (Vite). Copy to .env.local and adjust as needed.

# Leave empty in development — Vite proxies /api to http://localhost:4000
# (see vite.config.ts). In production, set this to the deployed API origin:
# VITE_API_URL="https://api.h-daily.com"
VITE_API_URL=
